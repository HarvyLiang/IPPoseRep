# Experiment reports

Store reviewed experiment summaries here. Each report should identify the run
id, Git commit, experiment config fingerprint, dataset manifest fingerprint,
seed, key metrics, and generation date so the conclusion can be traced back to
its evidence in `data/derived/experiment_runs/`.
