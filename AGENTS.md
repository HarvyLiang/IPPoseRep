# Workspace Rules

## 1. 基本原则

- 修改应聚焦当前需求，优先选择简单、清晰、可维护的实现，避免无关重构和过度设计。
- 开始工作前先了解相关代码、配置和文档；遵循仓库已有约定，除非需求明确要求调整。
- 不得覆盖或回退用户已有的无关改动。发现冲突时，应先说明影响再继续处理。
- 不得在仓库中写入密码、令牌、私钥或其他敏感信息；使用环境变量或本地忽略的配置文件。
- 新增依赖前应确认确有必要，并说明用途；优先使用现有依赖和标准库。

## 2. 项目架构

仓库必须保留以下顶层模块：

```text
configs/   项目配置、配置模板和可版本化的默认参数
data/      输入数据、样例数据及数据说明
docs/      架构、设计、使用说明、运行手册和决策记录
reports/   分析结果、实验总结和面向阅读者的交付报告
scripts/   可独立执行的开发、运维、数据处理和自动化脚本
src/       产品代码与核心业务逻辑
tests/     自动化测试
```

目录职责规则：

- `src/` 是可复用实现的唯一主要位置；不得把核心业务逻辑长期留在 `scripts/`、notebook 或测试文件中。
- `tests/` 的结构应尽量映射 `src/`，测试名称应清楚描述被测行为。
- `scripts/` 中的脚本应入口明确、参数可发现、失败时返回非零退出码；重复使用的逻辑应下沉到 `src/`。
- `configs/` 只保存可安全提交的配置与模板。环境专属值和密钥不得提交；必要时提供脱敏示例。
- `data/` 应区分原始数据与派生数据。大型数据、临时数据或可重新生成的数据默认不提交，并在文档中说明获取或生成方式。
- `reports/` 保存最终或阶段性结论；中间产物、缓存和调试输出不得随意堆放于此。
- `docs/` 中的说明应与实现同步更新。涉及架构、接口、配置方式或操作流程的变更，必须同步修改相关文档。
- 可按实际需要增加其他目录（如 `notebooks/`、`assets/`、`examples/`），但不得与上述模块职责重叠；新增顶层目录时应在此文件或相关文档中说明用途。

## 3. 代码与配置

- Python 代码遵循 `pyproject.toml` 中的版本、格式和静态检查配置。
- 公共接口、复杂逻辑和非显然的设计决策应有简洁说明；注释应解释原因，而不是复述代码。
- 配置项应有明确名称、合理默认值和必要的校验。影响兼容性的配置变更应同步更新示例与文档。
- 文件和模块应保持单一职责。若一个文件同时承担多个独立职责，应优先拆分为边界清楚的模块。
- 错误不得静默吞掉；应提供足以定位问题的上下文，同时避免在日志中泄露敏感数据。

## 4. 任务规模与执行方式

根据实际影响范围选择最轻量但足够可靠的流程，不为简单任务强制套用复杂工作流。

### 小任务

满足以下大多数特征时，视为小任务：修改范围局部、需求明确、风险较低、无需架构决策、通常只涉及少量文件。

- 可直接检查相关上下文并实施，不要求先创建计划文档。
- 完成后运行与改动直接相关的测试、静态检查或最小可行验证。
- 纯文档、注释或无行为变化的配置说明修改，可用内容检查代替全量测试。

### 大任务

出现以下任一情况时，通常应视为大任务：跨多个模块、引入新架构或依赖、修改公共接口或数据格式、涉及迁移或兼容性、需求存在多个阶段、失败影响较大。

- 实施前先编写可执行计划，明确目标、范围、关键决策、步骤、风险和验证方式。
- 将工作拆成可独立验证的阶段，并在执行过程中维护计划状态和重要发现。
- 涉及重要设计取舍时，先给出方案与权衡并取得确认，再进行高成本或难以回退的修改。
- 完成实现后必须运行全量自动化测试，并运行仓库配置的格式、静态检查和类型检查（若存在）。
- 若全量验证因环境、依赖、时长或外部服务无法完成，必须明确说明未运行项、原因、已完成的替代验证及剩余风险，不得宣称全部通过。

任务大小以影响和风险为准，不以改动行数作为唯一标准。小改动若影响认证、安全、数据迁移、公共 API 或生产配置，也应按大任务流程处理。

## 5. 测试与完成标准

- 新功能和缺陷修复原则上应增加或更新测试；测试应覆盖正常路径、关键边界和预期失败路径。
- 修复缺陷时，优先添加能复现问题的回归测试，再验证修复结果。
- 测试应确定、可重复，避免无必要地依赖真实网络、系统时间或外部服务。
- 验证强度应与风险相称：局部修改运行针对性测试；跨模块、发布前、依赖升级或高风险修改运行全量测试。
- 未实际运行的检查不得表述为“通过”。完成报告应简要列出已运行的验证及其结果。
- 工作只有在需求已满足、相关文档已同步、验证结果可说明且没有已知未披露风险时，才可标记为完成。

## 6. 产物与仓库卫生

- 缓存、临时文件、日志、模型权重、构建产物和本地环境文件应通过 `.gitignore` 排除，除非它们是明确要求版本化的交付物。
- 生成的报告或数据应能够追溯到输入、配置和生成脚本；重要结果应记录生成方式和日期。
- 不执行破坏性文件或 Git 操作，除非用户明确授权且目标已经核实。
- 提交内容应保持聚焦，不混入与当前任务无关的格式化、重命名或清理。

## 7. 项目级 Skills

- 本项目启用已安装的 `ponytail` skill。编码、修复、重构、代码审查、架构设计和依赖选择任务默认使用 `full` 强度；用户明确要求 `lite`、`ultra`、停止 Ponytail 或恢复普通模式时，以用户选择为准。具体实现规则见第 8 节。
- 本项目启用仓库内 `.agents/skills/ppt-master/` 的 `ppt-master` skill。涉及演示文稿、PPT、PPTX、幻灯片、课件的创建、重构、模板填充、美化或增强时，应优先使用该项目级 skill，并遵循其 `SKILL.md`。
- 本项目禁用 Superpowers 技能集，包括 `using-superpowers` 及其关联的规划、开发、调试、测试、审查和分支收尾工作流。即使这些技能在全局环境中可用，也不得在本项目中调用或依赖；工作流程以本文件及明确启用的项目级 skills 为准。

## 8. Ponytail 最小实现规则

在充分理解需求并追踪相关代码实际调用路径后，按以下顺序选择第一个能够正确满足需求的方案：

1. 确认功能是否真的需要存在；推测性的未来需求不实现。
2. 优先复用仓库中已有的 helper、类型、模块和模式。
3. 优先使用 Python 标准库。
4. 优先使用操作系统、数据库、浏览器或框架已有的原生能力。
5. 优先使用仓库中已经安装的依赖；不得为少量简单逻辑随意新增依赖。
6. 能用简单表达式或小函数清楚完成时，不引入额外抽象。
7. 只有上述方案均不满足时，才编写能够正确工作的最小实现。

具体规则：

- 不创建当前只有一个实现且没有明确扩展需求的接口、工厂或插件层。
- 不添加未被当前需求使用的配置项、兼容层、脚手架或预留代码。
- 优先删除和复用，选择朴素、清楚的实现；避免炫技式压缩和难以维护的“一行代码”。
- 在确认行为正确的前提下，以修改文件更少、差异更小的方案为优先。
- 缺陷修复应定位共同根因，并检查相关调用方；不得只修补报告中出现的单一路径而遗留同类问题。
- 若采用有明确容量上限的简化方案，应使用 `ponytail:` 注释说明限制和触发升级的条件。
- 不得以“最小实现”为由省略信任边界输入校验、防止数据丢失的错误处理、安全措施、可访问性要求、硬件校准能力或用户明确要求的功能。
- 非平凡逻辑至少保留一个能在行为破坏时失败的最小可运行检查；验证强度仍遵循本文件第 4、5 节。
- Ponytail 只约束实现复杂度，不限制用户明确要求的报告、说明或设计文档质量。

## 9. Skill 使用披露

- 每次实际使用任何 skill 时，必须在采取受该 skill 影响的行动前，通过面向用户的消息显式说明 skill 名称及本次用途。
- 同一任务使用多个 skills 时，应一次列出当前确定使用的全部 skills；后续新增 skill 时，在首次使用前补充说明。
- 不得声称使用未实际读取或执行的 skill，也不得以笼统的“使用相关技能”代替具体名称。
- Skill 披露应简短、清楚，不得因披露要求为小任务引入额外计划、文档或复杂流程。
